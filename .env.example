# Neon / PostgreSQL Configuration (for Vercel / Cloud)
DATABASE_URL=postgresql://user:password@ep-xyz.region.aws.neon.tech/neondb?sslmode=require
# or POSTGRES_URL

# MySQL Configuration (for Hostinger/Production)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=attendance_db
DB_PORT=3306

